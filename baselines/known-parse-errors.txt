# Files with pre-existing parse errors. EMPTY as of optimization round 2:
# the message-payload and require-feature-chain gaps are fixed, so every
# example parses cleanly. Add entries only with a documented root cause
# in BACKLOG.md.
